import 'package:flutter/material.dart';

void main() => runApp(MaterialApp(
  home: Iamps(),
));

class Iamps extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'i-AMPs',
      theme: ThemeData(
        
      ),
    );
  }
}


